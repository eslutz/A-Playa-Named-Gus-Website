{
  "name": "gus-website",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "test": "npm run build && node --test",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
